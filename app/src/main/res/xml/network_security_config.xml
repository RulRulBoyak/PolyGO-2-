<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production API endpoints require HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">polygo.pks.edu.my</domain>
    </domain-config>
    <!-- Release default: no cleartext. Debug exceptions live in src/debug. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
